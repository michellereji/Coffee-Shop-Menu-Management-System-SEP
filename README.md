# Coffee-Shop-Menu-Management-System-SEP
To build a small web application for a coffee shop to keep the records of its items. The application must store each item's Item Name, Category, Size, and Price. The user should be able to Add a new item, View all items, Edit a item's details, and Delete a item.
